# Clusterizacao

Notebook oficial:

1. `01_clusterizacao_filiais.ipynb`

Objetivo:

- ler os checkpoints da `Base_V2/`;
- testar configuracoes de clusterizacao;
- escolher a referencia de clusters;
- gerar perfil e validacoes dos clusters;
- salvar as saidas oficiais em `Base_V2/`.

Execute a partir da raiz do repositorio para manter `Path.cwd()` correto.
